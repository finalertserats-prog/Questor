node_modules/
dist/
build/
.next/
coverage/
*.log
.env
.env.local
.env.*.local
server/.env
web/.env
*.sqlite
*.sqlite-journal
*.db
server/prisma/*.db
server/prisma/dev.db*
server/data/
uploads/
tmp/
.DS_Store
.vite/
*.tsbuildinfo
playwright-report/
test-results/
artifacts/

# Interview simulation output: transcripts, judge verdicts and synthesised audio.
# Regenerated by `npm run sim -w server`; copy a report into docs/ to keep one.
server/sim-results/

# Local AI-harness session memory (not part of the project)
notebook/

# Operator credential drops. These land in the repo root by habit and are one
# `git add .` away from a public commit — see the 2026-04-23 incident.
App.txt
*.app-password
*credential*.txt
*secret*.txt
docs/Questor-v1-hr-guide-20260719.pdf.bak
